name: Keep Render Awake
on:
  schedule:
    - cron: '*/10 * * * *' # Runs every 10 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s https://nvs-demo-s2of.onrender.com/ > /dev/null
